import { connect } from "react-redux";
import Footer from './footer';

const msp = state => {

    return({

    });
};

const mdp = dispatch => {

    return({

    });
};

export default connect(msp, mdp)(Footer);