import { connect } from "react-redux";
import Search from './search';

const msp = (state) => {
    
    return({

    });
};

const mdp = (dispatch) => {
    
    return({

    });
};

export default connect(msp, mdp)(Search);